using UnityEngine;
using System.Collections;

public class EventLoader : MonoBehaviour {
	/*public const string path = "events";
	public EventContainer ec;
	void Start () {
		ec = EventContainer.Load (path);
		foreach(Event event_ in ec.events){
			print (event_.name);
		}
	}*/
}
